# 线上 / 离线操作一致性审计

基准页面：`https://cq-zys.cn/hero-lineup`  
核对日期：2026-07-23
状态含义：`一致`、`部分一致`、`缺失`、`待实测`。

本文件是迁移工作的验收矩阵。只有线上实际操作、离线实际操作和自动化回归三方都有证据的项目，才可标记为“一致”。

## 体系与主页面

| 操作 | 线上实测行为 | 离线当前行为 | 状态 |
|---|---|---|---|
| 我的体系 / 热门体系 | 两个页签；热门体系支持搜索，点击“查看体系”会复制到我的体系并立即切换 | 以“本地收藏”替代联网热门库；支持名称/描述搜索，“使用体系”复制为私有可编辑副本并立即切换 | 一致（离线语义适配） |
| 新增体系 | 先打开“创建新体系 / 口令导入”双页签弹窗；创建页填写名称、200 字描述、公开/私有，确认后才新增并切换 | 已采用相同双页签、字段、取消/确认与切换流程；口令页接受完整离线体系配置码 | 一致（口令载荷为离线格式） |
| 编辑体系 | 弹窗编辑名称、200 字描述和公开/私有；取消/保存 | 已采用同字段、单选项、计数及取消/保存弹窗；公开表示进入本地收藏 | 一致（离线语义适配） |
| 保存体系 | 明确保存当前未保存状态 | SQLite / 浏览器预览本地存储明确保存 | 一致 |
| 导入口令 / 导出口令 | 导出弹窗展示装饰文本与 6 位大写字母数字短码；导入严格校验 `/^[A-Z0-9]{6}$/`，登录后通过 `/api/hero-lineup/systems/shortcode/{code}` 从服务器取完整体系 | 已还原导出弹窗与复制流程；离线口令携带完整规范 JSON，可跨离线设备导入。检测到线上 6 位短码时明确说明其只含服务器索引，并引导先在线转换或使用 `.zyslineup` 文件 | 部分一致（任意线上短码无法在零网络、无服务器快照条件下解析） |
| 勇士阵容 | 13 名勇士始终显示、始终可拖拽，无“选中勇士”步骤 | 已取消点击勇士时切换入队的错误逻辑；所有勇士始终进入可用单位目录 | 一致 |
| 装备需求统计 | 英雄与勇士各自打开独立统计弹窗；按装备汇总“需要”数量，并允许录入“已有”数量 | 已改正原先错误打开配装模板库的行为；分别汇总当前体系英雄六槽和勇士专属装备，本机持久化已有数量 | 一致 |
| 英雄排序 | 职业排序 / 元素排序 | 两种排序均有 | 一致 |
| 冒险任务计数 | 标题显示 `已用/48` | 已显示计数，并在添加分组、添加任务和克隆任务三条路径统一限制 48 | 一致 |

## 英雄创建与英雄编辑器

| 操作 | 线上实测行为 | 离线当前行为 | 状态 |
|---|---|---|---|
| 选择职业 | 42 个职业，按线上目录顺序展示职业和元素图标 | 42 个本地职业与本地图标；已按线上完整顺序加生产目录断言，并修正时空法师/织命者末尾次序 | 一致（仍需像素核对） |
| 选择创建模板 | 选择职业后必经模板弹窗；总有空白模板，并显示服务器预设与 4 个技能预览 | 已新增同流程；44 条在线预设已快照到本地，运行时零网络访问 | 一致 |
| 模板数据维护 | 线上 API `hero-templates?heroClass=...` | `npm run templates:snapshot` 可重建本地快照 | 一致（维护方式为离线适配） |
| 克隆英雄 | 立即新增同职业下一个编号，并切换编辑器到克隆英雄 | 已按同一行为实现 | 一致 |
| 上一个 / 下一个 | 首尾循环切换英雄 | 已按同一行为实现 | 一致 |
| 修改自动保存 | 每次用户修改自动计算并写回体系，关闭不丢失 | 已移除“保存配装”分叉状态；计算完成后自动写回；关闭时在途计算仍会完成写回 | 一致 |
| 导入 | 固定显示“粘贴配置码”输入框，可导入线上英雄配置码 | 已显示同输入框；兼容线上配置码和旧离线剪贴板格式 | 一致 |
| 导出 | 复制线上兼容英雄配置码 | 已输出线上兼容格式，并保留旧格式导入兼容 | 一致 |
| 导出图片 | 以 2× 像素比直接截取当前编辑器内容；先打开“英雄配装图片预览”，再复制或下载 | 已改为相同真实 DOM 截图、预览、复制和下载流程；不再另画简化统计图 | 一致（已通过真实 Chromium 图片预览截图核对） |
| 英雄改名 | 点击名称进入改名交互；中文按 2、ASCII 按 1 计，最多 12 | 已实现点击改名、Enter/失焦提交、Escape 取消和同权重长度限制 | 一致 |
| 英雄等级 / 最大阶数 | 等级 1–50；最大装备阶数自动显示 | 已实现 | 一致 |
| 种子 / 卡片 | 种子 0–80；卡片 0–3 | 已实现 | 一致 |
| 自带技能 | 按职业固定；卡片显示一级技能族名称，等级徽标和效果随元素值提升；数值效果与职业特殊机制合并展示 | 已按归档线上 `tr + sr` 两段生成逻辑投影 `innateEffects`，覆盖42职业的佣兵、狂战士、忍者、咒术骑士等特殊文案，并通过一级/上限和元素边界契约 | 一致 |
| 后四技能 | 按职业槽位和英雄等级解锁；英雄等级只控制槽位，技能等级由元素值与职业上限解析；其他槽已选技能族或技能类别从目录隐藏，当前槽技能仍可重新选择，界面没有额外“清空技能”入口 | 已实现相同职业筛选、解锁、元素等级、目录排序、技能族/类别互斥及导入归一化，并移除离线独有的禁用卡片与清空按钮 | 一致（仍持续增加职业金样本） |

## 装备与实时计算

| 操作 | 线上实测行为 | 离线当前行为 | 状态 |
|---|---|---|---|
| 六槽顺序 | 武器、身体、手部、头部、脚部、饰品 | 已修正并迁移旧错误顺序 | 一致 |
| 装备目录与排序 | 按槽位职业限制展示；阶数降序、解锁等级降序、原始数据顺序升序 | 本地图鉴使用相同三级排序，且按英雄最大装备阶数过滤 | 一致 |
| 品质 | 普通、高级、无暇、史诗、传奇 | 内部规范映射正确；显示标签一致 | 一致 |
| 星能铸造 / 超越 / 品质 | 空槽时控件作为下一件装备的候选配置；已有装备时切换会立即改写当前装备并刷新属性；打开已有装备槽会恢复该槽配置，空槽沿用最近一次筛选配置 | 已改为同样的即时写回与筛选继承语义，不再等待额外“完成选择”提交 | 一致 |
| 全部应用 | 主装备选中后显示星能、超越、品质三个按钮；元素/精萃实际选中后才各自显示；只改写已穿装备，且不覆盖装备内置附魔 | 五类批量操作、条件显示、空槽跳过和 `lTag2/lTag3` 保护均已实现 | 一致 |
| 主装备 / 元素 / 精萃反选 | 点击候选立即选择；再次点击当前卡片即清空，不提供额外“不装备 / 无元素 / 无精萃”卡片；右上“关闭”只关闭弹窗 | 已移除离线独有的三个清空卡片、搜索框、底部“完成选择”和自定义名称区，并采用相同再次点击反选语义 | 一致 |
| 元素附魔 / 精萃附魔 | 两列独立选择，影响元素、技能等级和属性；装备内置 `lTag2/lTag3` 自动生效且获得 1.5 倍内置亲和 | 已实现本地图鉴选择、即时属性刷新、Rust 最终计算、亲和标记和内置附魔保护 | 一致（特殊精萃效果仍受已知计算边界约束） |
| 泰坦之塔 / 墓 | 编辑器属性区顶部单按钮切换选中态，并实时影响属性 | 已按同位置、文案和选中态还原，继续复用本地墓附魔计算 | 一致 |
| 实时属性 | 穿脱装备、技能、等级等都立即更新；校验提示不阻断编辑状态保存 | 已修复装备配置与 Rust 结果同步，并移除“存在校验错误便拒绝写回”的离线分叉；无效项保留配置但不计入属性 | 一致 |
| 编辑器总体布局 | 左侧头像与纵向等级参数、右侧技能；分隔线下左侧实时属性、右侧装备；暴击同时显示暴击率/暴击伤害 | 英雄与勇士编辑器已重排为相同两段双栏结构，使用本地属性图标；勇士补齐元素值，离线模板与校验工具不再占据线上主流程位置 | 一致（仍需真实资源环境逐像素核对） |

## 勇士编辑器

| 操作 | 线上实测行为 | 离线当前行为 | 状态 |
|---|---|---|---|
| 前后勇士切换 | 首尾循环 | 已实现 | 一致 |
| 等级 / 最大阶数 / 阶数 | 等级 1–50；阶数含 `11+n` 展示 | 已实现 | 一致 |
| 勇士之魂 | 独立开关 | 已实现 | 一致 |
| 团队技能 | 随阶数自动升级，不可替换 | 已实现 4 级解析和效果展示 | 一致 |
| 两个专属装备槽 | 使魔、光环；与英雄装备相同采用即时选择/反选和筛选继承；选中主装备后显示星能/超越/品质/元素 4 个“全部应用”，选中精萃后增至 5 个 | 已实现相同槽位命名、即时写回、选择反选、条件按钮和内置附魔保护 | 一致 |
| 导入 / 导出 | 文本配置码 | 已实现线上 `championId/level/rank/seed/card/equipment` 配置码，并保留旧离线格式导入 | 一致 |
| 导出图片 | 与英雄相同，以 2× 像素比截取当前勇士编辑器并先预览 | 已复用相同真实 DOM 截图、预览、复制和下载流程 | 一致 |
| 自动保存 | 修改立即写回体系 | 已实现 | 一致 |

## 冒险任务与模拟

| 操作 | 线上实测行为 | 离线当前行为 | 状态 |
|---|---|---|---|
| 添加分组 | 新建含默认“流星区”的无标题任务组；最后一个任务删除后分组自动消失 | 已去除本地分组名称、上下移动和整组删除工具栏；最后任务删除或移走后自动移除空组 | 一致 |
| 地图选择 | 点击左上地图图标打开图片网格；普通、黄金城、泰坦塔、快闪四页签。普通/快闪地图进入四难度页，黄金城进入 1–13 难度页；泰坦塔先选第 1–30 层或泰坦之墓，再选该层的六种/墓穴的五种变体 | 已按线上顺序、两级流程和独立图片资源实现；地图缩略图、普通/黄金难度图标及泰坦背景+变体双图层分别投影，不再复用地图图片充当难度图片 | 一致（仍需逐分类像素核对） |
| 克隆 / 删除任务 | 任务卡内操作 | 已有 | 一致 |
| 添加任务 | 点击组末尾“添加任务”先打开地图/难度选择，选择完成后才创建任务 | 已复用同一地图/难度选择器；关闭选择器不会创建任务 | 一致 |
| 队伍编排 | 英雄/勇士拖入任务；点击 `+` 打开“选择成员添加到任务”模态目录；可切换“未上阵成员”；选择后关闭；圆形头像、元素角标和 `×` 均在卡内展示 | 已改为相同模态目录、默认关闭的全局未上阵筛选、选择即关闭和紧凑成员图标流程 | 一致 |
| 强化道具 | 卡内 `+` 打开“冒险强化道具”三卡弹窗；再次点击已选卡取消 | 已还原相同入口、三卡弹窗、选中/取消语义与三档效果 | 一致 |
| 精英怪 | 卡内紧凑下拉：无、敏捷、巨大、凶残、富有、传奇 | 已改为相同互斥下拉并持久化具体类型 | 一致 |
| 元素屏障 | 卡内显示任务所有候选元素与队伍有效元素值/需求值；紧凑下拉的 `selectedElement` 为自动、任务可选元素或 `force` 无屏障 | 目录已保留逗号分隔的全部候选元素；任务卡显示候选图标与有效值；Rust 随机屏障只在任务候选内取队伍最大元素值，且不再错误使用攻击+防御代替元素 | 一致 |
| 测试冒险 | 点击后直接运行固定 10,000 次；卡片仅显示三位小数成功率与“查看详情/测试冒险” | 已固定 10,000 次并改成相同的直接重算和紧凑结果行；取消和 PNG 导出保留在离线详情能力中 | 一致（数值基准仍需更多金样本） |
| 模拟详情 | 地图/难度、三类选项、五项汇总、成员存活率/伤害/剩余生命及完整配置；打开时自动以 2× 像素比预生成详情图，可复制/下载，点击职业图标复制配置码 | Rust 已返回成员级结果；详情包含完整技能、等级/种子/卡片、实时属性、装备卡；现同样自动预生成真实 DOM 图片，准备完成前禁用复制/下载 | 一致（已通过 1440×900 详情截图与交互回归核对） |

## 当前自动化证据

- 前端单元/交互测试：47 项通过；包含新增体系双页签、线上六位短码离线边界提示、导出口令预览、本地收藏复制、任务组自动清理、添加任务选择流程、成员模态目录与全局未上阵筛选、装备需求统计、装备亲和标记、泰坦变体保存恢复、五类“全部应用”和“存在校验错误仍保存装备与计算属性”的回归。
- 浏览器端到端：20 项通过，覆盖体系创建/收藏、英雄与勇士配装、装备需求统计、自动同步、四类地图选择、泰坦两级选择及图片实际加载、任务选项、任务拖放和模拟流程。
- Rust `hero-catalog`：已覆盖品质、星能、超越、附魔、技能和真实数据金样本；除 `forestdagger` 与 `t16sword` 外，新增当前线上 T16 攻击/防御/生命三类四阶段卡片数值与穿戴总属性断言。
- Tauri 命令层与内容包：11 项测试通过。

## 技能与装备目录补充核对

- 线上技能选择器先按稀有度降序，同稀有度按原始 `skills.json` 数据逆序；离线目录现由 Rust 保留并透传 `sourceOrder`，不再用中文名称或 `rank` 猜测排序。
- 线上士兵样本确认：编辑空槽时，其他槽已选的技能族不会以禁用态出现，而是从目录完全隐藏；编辑当前槽时该技能仍正常出现，点击后保持选择并关闭弹窗，且没有“清空技能”按钮。
- 线上归档选择器还会隐藏其他槽已经占用的非空 `category`；离线目录、配置码导入和 Rust 最终计算现共同执行技能族与类别两层互斥，并丢弃未解锁槽位中的导入技能。
- 元素等级金样本：士兵的“战争大师”在 70 元素时为 2 级、105 元素时为 3 级，210 元素仍受基础职业 3 级上限约束；离线 Rust 已加入对应边界断言，晋升职业仍可到 4 级。
- 线上装备先按阶数降序，同阶按装备解锁等级降序，最后保留原始 `items.json` 顺序；离线装备选择器已采用相同规则，并按英雄等级隐藏超过“最大装备阶数”的装备。
- 装备的 `elementAffinity` / `spiritAffinity` 与内置 `lTag2` / `lTag3` 均由 Rust 规则引擎按线上规则计算；内置附魔自动生效并按 1.5 倍计算，离线六槽卡片也显示“元素亲和/精萃亲和”标记。
- 2026-07-23 当前线上 T16 金样本：`杀戮与恶意`攻击为 `1780 → 5340 → 6675 → 7930`（普通、传奇、传奇星能、传奇星能超越），`遗忘的墓碑`防御为 `775 → 2325 → 2906 → 3924`且超越生命 `+470`，`怨念混合物`生命为 `171 → 513 → 641 → 867`且超越攻击 `+348`。三件同时穿戴后的士兵总属性为攻击 `8628`、防御 `4374`、生命 `1637`、暴击 `12%`。
- 当前线上装备弹窗已无搜索框、显式清空卡和底部“完成选择”；选中状态即时写回，点击当前卡片反选。离线英雄与勇士装备弹窗已同步此交互，不再保留旧暂存分叉。
- 当前线上 `space04`（流星区）屏障为暗/光/土三候选、需求 320；离线生产目录测试锁定三候选投影，任务卡和模拟输入共同使用真实 `elementValue`。
- 快闪目录已按线上 17 项固定顺序投影，并使用 `fq_{family}_name` 中文键；泰坦塔第 1 层的阿尔法、贝塔、伽马、德尔塔、艾普斯龙、奇异，以及泰坦之墓的五种墓穴变体均保留独立任务 ID，保存/重载后不会退化成楼层名。

## 下一轮优先级

1. 继续用本地构建截图逐像素核对地图选择器各分类和多成员模拟详情中的配置卡排列。
2. 若需要直接迁移任意线上六位口令，增加一个用户主动执行的“在线转完整离线包”迁移助手；运行期离线应用本身继续保持零网络访问。
